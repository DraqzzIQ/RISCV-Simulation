name: Build and Release

on:
  push:
    branches:
      - master
      - dev
  pull_request:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ macos-latest, windows-latest, ubuntu-latest ]
        compiler: [ gcc ] # Specify your desired compiler here

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodules: true
          fetch-depth: 0

      - name: Update submodules
        run: git submodule update --init --recursive

      - name: Set up CMake
        uses: lukka/get-cmake@latest

      - name: Install dependencies
        uses: jurplel/install-qt-action@v4
        with:
          version: '6.6.*'

      - name: Configure CMake
        run: cmake -S . -B build

      - name: Build
        run: cmake --build build

      - name: Run tests
        run: ctest --test-dir build/tests --output-on-failure -C Debug -V

      - name: Package build
        run: |
          mkdir -p release
          cp -r build/* release/

  release:
    permissions:
      contents: write
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/dev'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Determine if prerelease
        id: prerelease
        run: echo "::set-output name=prerelease::$(if [ '${{ github.ref }}' == 'refs/heads/dev' ]; then echo true; else echo false; fi)"

      - name: Create Release
        id: create_release
        uses: ncipollo/release-action@v1
        with:
          artifacts: release/
          draft: false
          prerelease: ${{ steps.prerelease.outputs.prerelease }}